#include "main.h"

/**
 * mul - multiply the value of to numbers
 *
 * @a: number to be multiply
 *
 * @b: number to be multiply
 *
 * Return: the product of a and b
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
